  // data type specific perhaps
  int Nzp = Nz + 1;

  // standardise
  // real z_min = x_min / x_sd;
  // real z_range = x_range / x_sd;
  // vector[N] z = x / x_sd;
  // vector[Nrep] zrep = xrep / x_sd;
